spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/backoffice?createDatabaseIfNotExist=true
    username: root
    password: password

server:
  error:
    include-stacktrace: never

api:
  security:
    token:
      secret: ${SECRET_JWT:98hb5PeR43EWqd2v10}